/**
 * ArtifactsTab - Artifacts inventory as a sub-tab of Roster
 *
 * Re-exports the ArtifactsPage component for use within the Roster layout.
 * The component handles its own loading, filtering, and display logic.
 */
export { default } from '@/features/artifacts/pages/ArtifactsPage';
